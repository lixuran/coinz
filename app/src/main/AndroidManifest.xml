<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ilpcoursework.coinz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
     
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.MapboxActivity" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".activities.CoinActivity"
            android:label="@string/title_activity_coin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.MywalletActivity2"
            android:label="@string/title_activity_mywallet2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.SignupActivity2"
            android:label="@string/title_activity_signup2" />
        <activity android:name=".activities.welcomingActivity" />
        <activity
            android:name=".activities.walletselectActivity"
            android:label="@string/title_activity_walletselect"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.profileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".mapboxActivity2"
            android:label="@string/title_activity_mapbox2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.myfriendActivity"
            android:label="@string/title_activity_myfriend"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>